spring:
  datasource:
    username: root
    password: password
    url: jdbc:h2:mem:exchange-rates;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop